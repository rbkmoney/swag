required:
  - cardNumberMask
  - paymentSystem
properties:
  cardNumberMask:
    description: Маскированый номер карты
    type: string
    pattern: '^\d{2,4}$'
  paymentSystem:
    x-rebillyMerge:
      - $ref: '#/definitions/BankCardPaymentSystem'
