required:
  - cardNumberMask
  - paymentSystem
properties:
  cardNumberMask:
    description: Маскированый номер карты
    type: string
    pattern: '^\d{2,4}$'
  cardBin:
    description: BIN карты
    type: string
    pattern: '^\d{6}$'
  paymentSystem:
    x-rebillyMerge:
      - $ref: '#/definitions/BankCardPaymentSystem'
