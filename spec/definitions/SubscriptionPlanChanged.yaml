type: object
allOf:
  - $ref: '#/definitions/SubscriptionChange'
  - type: object
    required:
      - plan
    properties:
      plan:
        description: Идентификатор плана подписки
        type: string
