description: Параметры комиссии в относительных значениях
allOf:
  - $ref: '#/definitions/AllocationBodyTotalParams'
  - type: object
    required:
      - shareFeeAmount
    properties:
      shareFeeAmount:
        $ref: '#/definitions/Decimal'
