type: object
allOf:
  - $ref: '#/definitions/PaymentToolDetails'
  - type: object
    required:
      - provider
    properties:
      provider:
        x-rebillyMerge:
          - $ref: '#/definitions/TerminalPaymentProvider'
