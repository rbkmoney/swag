type: object
allOf:
  - $ref: '#/definitions/PaymentToolDetails'
  - type: object
    required:
      - provider
    properties:
      provider:
        type: string
        enum:
          - euroset
