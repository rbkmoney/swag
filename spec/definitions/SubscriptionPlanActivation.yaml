description: Правила подключения подписки
type: object
required:
  - amount
  - enabled
properties:
  amount:
    description: Сумма платежа за подключение
    type: integer
  enabled:
    description: Активность правила
    type: boolean
