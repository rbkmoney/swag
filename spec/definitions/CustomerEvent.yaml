type: object
required:
  - id
  - createdAt
  - changes
properties:
  id:
    type: integer
  createdAt:
    type: string
    format: date-time
  changes:
    type: array
    items:
      $ref: '#/definitions/CustomerChange'
