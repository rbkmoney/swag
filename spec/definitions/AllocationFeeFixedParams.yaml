description: Параметры комиссии в абсолютных значениях
allOf:
  - $ref: '#/definitions/AllocationBodyTotalParams'
  - type: object
    required:
      - feeAmount
      - feeCurrency
    properties:
      feeAmount:
        description: >
          Значение комиссии в минорных денежных
          единицах,

          например в копейках в случае указания российских рублей в качестве
          валюты.
        type: integer
        format: int64
        minimum: 1
      feeCurrency:
        x-rebillyMerge:
          - $ref: '#/definitions/Currency'
