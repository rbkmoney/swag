type: object
allOf:
  - $ref: '#/definitions/PaymentTool'
discriminator: paymentToolType
properties:
  paymentToolType:
    description: Тип платежного средства
    type: string
    enum:
      - CardData
      - PaymentTerminalData
      - DigitalWalletData
      - MobilePaymentToken
required:
  - paymentToolType
