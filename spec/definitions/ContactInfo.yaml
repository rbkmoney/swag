description: Контактные данные плательщика
type: object
properties:
  email:
    x-rebillyMerge:
      - $ref: '#/definitions/Email'
  phoneNumber:
    x-rebillyMerge:
      - $ref: '#/definitions/PhoneNumber'
