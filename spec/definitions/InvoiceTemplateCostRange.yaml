type: object
allOf:
  - $ref: '#/definitions/InvoiceTemplateCost'
  - type: object
    required:
      - currency
      - range
    properties:
      currency:
        x-rebillyMerge:
          - $ref: '#/definitions/Currency'
      range:
        $ref: '#/definitions/CostAmountRange'
