type: object
required:
  - status
properties:
  status:
    description: Статус привязки
    type: string
    enum:
      - pending
      - succeeded
      - failed
  error:
    $ref: '#/definitions/CustomerBindingError'
