type: object
required:
  - status
properties:
  status:
    description: Статус привязки
    type: string
    enum:
      - creating
      - pending
      - succeeded
      - failed
  error:
    $ref: '#/definitions/CustomerBindingError'
