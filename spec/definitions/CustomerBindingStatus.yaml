type: object
required:
  - status
properties:
  status:
    description: Статус привязки
    type: string
    enum:
      - created
      - succeeded
      - failed
  reason:
    description: Причина провала привязки
    type: string
    maxLength: 1000
