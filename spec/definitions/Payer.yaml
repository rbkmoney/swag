type: object
discriminator: payerType
properties:
  payerType:
    description: Тип платежного средства
    type: string
    enum:
      - CustomerPayer
      - PaymentResourcePayer
      - RecurrentPayer
required:
  - payerType
