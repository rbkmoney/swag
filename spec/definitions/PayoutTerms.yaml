type: object
properties:
  schedules:
    type: array
    items:
      description: Идентификаторы доступных расписаний выводов
      type: integer
      format: int32
  methods:
    type: array
    items:
      description: Доступные способы вывода средств
      type: string
      enum:
        - BankAccount
        - InternationalBankAccount
