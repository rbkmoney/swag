type: object
allOf:
  - $ref: '#/definitions/TokenizedCardData'
  - type: object
    description: Платежные данные Samsung Pay
    required:
      - serviceID
      - merchantID
    properties:
      serviceID:
        description: Идентификатор сервиса в Samsung Pay
        type: string
      merchantID:
        description: Идентификатор мерчанта в Samsung Pay
        type: string
