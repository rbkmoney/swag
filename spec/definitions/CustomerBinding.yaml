type: object
allOf:
  - $ref: '#/definitions/CustomerBindingStatus'
  - type: object
    required:
      - id
      - paymentResourceData
    properties:
      id:
        description: Идентификатор привязки
        type: string
      paymentResourceData:
        $ref: '#/definitions/DisposablePaymentResourceData'
