type: object
allOf:
  - $ref: '#/definitions/CustomerBindingStatus'
  - type: object
    required:
      - id
      - customerID
    properties:
      id:
        description: Идентификатор привязки
        type: string
      customerID:
        description: Идентификатор customer'а, в рамках которого была создана привязка
        type: string
