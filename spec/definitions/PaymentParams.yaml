type: object
required:
  - flow
  - payer
properties:
  externalID:
    x-rebillyMerge:
      - $ref: '#/definitions/ExternalID'
  flow:
    $ref: '#/definitions/PaymentFlow'
  payer:
    $ref: '#/definitions/Payer'
  paymentDeadline:
    x-rebillyMerge:
      - $ref: '#/definitions/PaymentDeadline'
  makeRecurrent:
    x-rebillyMerge:
      - $ref: '#/definitions/PaymentMakeRecurrent'
  metadata:
    description: Метаданные, которые необходимо связать с платежом
    type: object
