type: object
required:
  - flow
  - payer
properties:
  flow:
    $ref: '#/definitions/PaymentFlow'
  payer:
    $ref: '#/definitions/Payer'
  makeRecurrent:
    x-rebillyMerge:
      - $ref: '#/definitions/PaymentMakeRecurrent'
