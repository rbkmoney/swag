type: object
required:
  - flow
  - payer
properties:
  externalID:
    x-rebillyMerge:
      - $ref: '#/definitions/ExternalID'
  flow:
    $ref: '#/definitions/PaymentFlow'
  payer:
    $ref: '#/definitions/PayerParams'
  processingDeadline:
    x-rebillyMerge:
      - $ref: '#/definitions/ProcessingDeadline'
  makeRecurrent:
    x-rebillyMerge:
      - $ref: '#/definitions/PaymentMakeRecurrent'
  metadata:
    description: Метаданные, которые необходимо связать с платежом
    type: object
