type: object
required:
  - flow
  - payer
properties:
  externalID:
    x-rebillyMerge:
      - $ref: '#/definitions/ExternalID'
  flow:
    $ref: '#/definitions/PaymentFlow'
  payer:
    $ref: '#/definitions/Payer'
  businessDeadline:
    x-rebillyMerge:
      - $ref: '#/definitions/BusinessDeadline'
  makeRecurrent:
    x-rebillyMerge:
      - $ref: '#/definitions/PaymentMakeRecurrent'
  metadata:
    description: Метаданные, которые необходимо связать с платежом
    type: object
