type: object
required:
  - flow
  - payer
properties:
  flow:
    $ref: '#/definitions/PaymentFlow'
  payer:
    $ref: '#/definitions/Payer'
  isFirstRecurrent:
    description: Признак первого платежа в серии рекуррентных
    type: boolean
    default: false
