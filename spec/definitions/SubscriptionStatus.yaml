description: Статус подписки
type: object
readOnly: true
required:
  - status
properties:
  status:
    type: string
    enum:
      - Active
      - Inactive
  reason:
    type: string
    description: Причина остановки подписки
    enum:
      - error
      - customer paused
      - merchant paused
      - period over
