description: Статус подписки
type: object
readOnly: true
discriminator: subStatus
required:
  - subStatus
properties:
  subStatus:
    type: string
    enum:
      - SubscriptionActive
      - SubscriptionInactive
