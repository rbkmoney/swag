type: object
discriminator: paramsType
required:
  - currency
  - paramsType
properties:
  currency:
    description: Валюта, в которой производится вывод
    type: string
  paramsType:
    type: string
