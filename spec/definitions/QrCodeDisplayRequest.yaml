type: object
allOf:
  - $ref: '#/definitions/UserInteraction'
  - type: object
    required:
      - qrCode
    properties:
      qrCode:
        description: Содержимое QR-кода, записанное в виде потока байт
        type: string
        format: binary
