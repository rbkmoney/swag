description: Правила рекуррентности подписки
type: object
required:
  - amount
  - schedule
  - lifetime
properties:
  amount:
    description: Cумма повторяемого платежа
    type: integer
  schedule:
    $ref: '#/definitions/TimePeriod'
  lifetime:
    description: Продолжительность действия подписки
    oneOf:
      - type: string
        enum:
          - infinite
      - type: object
        $ref: '#/definitions/TimePeriod'
