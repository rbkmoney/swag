type: object
required:
  - id
  - status
  - changeset
properties:
  id:
    type: integer
    format: int64
  status:
    $ref: '#/definitions/ClaimStatus'
  changeset:
    $ref: '#/definitions/PartyChangeset'
