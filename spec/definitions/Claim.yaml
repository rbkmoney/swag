type: object
discriminator: status
required:
  - id
  - revision
  - status
  - changeset
properties:
  id:
    description: Идентификатор заявки
    type: integer
    format: int64
  revision:
    $ref: '#/definitions/ClaimRevision'
  status:
    description: Статус заявки
    type: string
  changeset:
    $ref: '#/definitions/ClaimChangeset'
