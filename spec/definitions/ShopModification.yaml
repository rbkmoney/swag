type: object
allOf:
  - $ref: '#/definitions/PartyModification'
  - type: object
    discriminator: shopModificationType
    required:
      - shopModificationType
      - shopID
    properties:
      shopID:
        type: integer
        format: int32
      shopModificationType:
        type: string
