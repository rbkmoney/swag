type: object
required:
  - guaranteeID
  - settlementID
  - currency
properties:
  guaranteeID:
    type: integer
    format: int64
  settlementID:
    type: integer
    format: int64
  currency:
    type: string
    format: '^[A-Z]{3}$'
