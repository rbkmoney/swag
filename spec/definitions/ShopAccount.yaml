description: Счета магазина
type: object
required:
  - currency
  - guaranteeID
  - settlementID
properties:
  currency:
    x-rebillyMerge:
      - $ref: '#/definitions/Currency'
  guaranteeID:
    type: integer
    format: int64
  settlementID:
    type: integer
    format: int64
