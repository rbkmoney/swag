type: object
required:
  - guaranteeID
  - settlementID
  - currency
properties:
  guaranteeID:
    $ref: '#/definitions/AccountID'
  settlementID:
    $ref: '#/definitions/AccountID'
  currency:
    type: string
    format: '^[A-Z]{3}$'
