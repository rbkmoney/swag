type: object
required:
  - paymentTool
  - clientInfo
properties:
  externalID:
    $ref: '#/definitions/ExternalID'
  paymentTool:
    $ref: '#/definitions/PaymentTool'
  clientInfo:
    $ref: '#/definitions/ClientInfo'
