description: Создание новых счетов в магазине
type: object
allOf:
  - $ref: '#/definitions/ShopModification'
  - type: object
    required:
      - currency
    properties:
      currency:
        description: Валюта создаваемых счетов
        type: string
