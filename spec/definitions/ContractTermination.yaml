type: object
allOf:
  - $ref: '#/definitions/ContractModification'
  - type: object
    required:
      - terminatedAt
    properties:
      terminatedAt:
        type: string
        format: date-time
      reason:
        type: string
        maxLength: 1000
