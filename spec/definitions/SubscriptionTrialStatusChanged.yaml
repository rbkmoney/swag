type: object
allOf:
  - $ref: '#/definitions/SubscriptionChange'
  - type: object
    required:
      - plan
    properties:
      plan:
        $ref: '#/definitions/SubscriptionPlan'
  - type: object
    required:
      - trialStatus
    properties:
      trialStatus:
        description: Статус триального периода
        type: string
        enum:
          - started
          - expired
