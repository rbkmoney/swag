type: object
required:
  - id
  - ownAmount
  - availableAmount
  - currency
properties:
  id:
    $ref: '#/definitions/AccountID'
  ownAmount:
    type: integer
  availableAmount:
    type: integer
  currency:
    type: string
