type: object
required:
  - id
  - ownAmount
  - availableAmount
  - currency
properties:
  id:
    type: integer
    format: int64
  ownAmount:
    type: integer
  availableAmount:
    type: integer
  currency:
    x-rebillyMerge:
      - $ref: '#/definitions/Currency'
