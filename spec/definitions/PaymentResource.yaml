type: object
description: Данные одноразового платежного средства
required:
  - contactInfo
properties:
  paymentToolToken:
    description: DEPRECATED! Токен платежного средства, предоставленного плательщиком
    type: string
    maxLength: 1000
  paymentSession:
    description: DEPRECATED! Идентификатор платежной сессии
    type: string
    maxLength: 1000
  paymentToolDetails:
    $ref: '#/definitions/PaymentToolDetails'
  clientInfo:
    $ref: '#/definitions/ClientInfo'
  contactInfo:
    $ref: '#/definitions/ContactInfo'