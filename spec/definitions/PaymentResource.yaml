type: object
description: Данные одноразового платежного средства
properties:
  paymentToolToken:
    description: >
      **Depricated.** _В будущем данный параметр возвращаться не будет._

      Токен платежного средства, предоставленного плательщиком
    type: string
    maxLength: 1000
  paymentSession:
    description: >
      **Depricated.** _В будущем данный параметр возвращаться не будет._

      Идентификатор платежной сессии
    type: string
    maxLength: 1000
  paymentToolDetails:
    $ref: '#/definitions/PaymentToolDetails'
  clientInfo:
    $ref: '#/definitions/ClientInfo'
