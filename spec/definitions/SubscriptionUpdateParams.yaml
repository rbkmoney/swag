desription: Изменение информации о подписке
type: object
properties:
  plan:
    $ref: '#/parameters/planID'
  status:
    $ref: '#/definitions/SubscriptionStatus'
    readonly: true
  paymentResource:
      $ref: '#/definitions/PaymentResource'
  email:
      description: 'Email, указанный при оплате'
      type: string
      format: email
      maxLength: 100
