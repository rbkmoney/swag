type: object
allOf:
  - $ref: '#/definitions/PaymentMethod'
  - type: object
    required:
      - cryptoCurrencies
    properties:
      providers:
        description: Список криптовалют
        type: array
        items:
          x-rebillyMerge:
            - $ref: '#/definitions/CryptoCurrency'
