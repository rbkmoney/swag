type: object
allOf:
  - $ref: '#/definitions/CustomerChange'
  - $ref: '#/definitions/CustomerBindingStatus'
  - type: object
    required:
      - bindingID
    properties:
      bindingID:
        type: string
