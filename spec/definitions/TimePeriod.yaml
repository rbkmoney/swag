description: Период времени
type: object
required:
  - metric
  - amount
properties:
  metric:
    description: Единица измерения
    type: string
    enum:
      - day
      - month
      - year
  amount:
    description: Количество
    type: integer
