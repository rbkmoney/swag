type: object
required:
  - status
properties:
  status:
    description: Статус рекуррентного токена
    type: string
    enum:
      - pending
      - acquired
      - failed
      - suspended
  error:
    $ref: '#/definitions/LogicError'
