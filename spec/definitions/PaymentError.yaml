description: |
  [Ошибка, возникшая в процессе проведения платежа](#tag/Error-Codes)
type: object
required:
  - code
properties:
  code:
    description: Основной код ошибки
    type: string
  subError:
    description: Описание ошибки более детального уровня
    type: object
    required:
      - code
    properties:
      code:
        description: Код ошибки более детального уровня
        type: string
      subError:
        $ref: '#/definitions/PaymentError/properties/subError'
