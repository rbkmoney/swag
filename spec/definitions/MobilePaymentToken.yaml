type: object
allOf:
  - $ref: '#/definitions/PaymentTool'
  - type: object
    description: Платежный токен мобильной системы
    discriminator: mobilePaymentTokenType
    properties:
      mobilePaymentTokenType:
        description: Тип платежного токена для мобильных платежей
        type: string
        enum:
          - ApplePay
          - SamsungPay