type: object
allOf:
  - $ref: '#/definitions/InvoiceChange'
  - $ref: '#/definitions/RefundStatus'
  - type: object
    required:
      - paymentID
      - refundID
    properties:
      paymentID:
        type: string
      refundID:
        type: string
