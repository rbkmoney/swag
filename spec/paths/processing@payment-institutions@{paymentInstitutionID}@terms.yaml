get:
  description: Получить условия проведения платежей по-умолчанию для платёжной организации
  tags:
    - PaymentInstitutions
  operationId: getPaymentInstitutionTerms
  parameters:
    - $ref: '#/parameters/requestID'
    - name: paymentInstitutionID
      in: path
      description: Payment institution reference
      required: true
      type: integer
      format: int32
  responses:
    '200':
      description: Payment institution terms calculated
      schema:
        $ref: '#/definitions/PaymentInstitutionTerms'
    '400':
      $ref: '#/responses/BadRequest'
    '404':
      $ref: '#/responses/NotFound'
