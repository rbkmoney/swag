post:
  description: Создать нового customer'а.
  tags:
    - Customers
  operationId: createCustomer
  parameters:
    - $ref: '#/parameters/requestID'
    - name: customerParams
      description: Параметры создаваемого customer'а
      in: body
      required: true
      schema:
        $ref: '#/definitions/CustomerParams'
  responses:
    '201':
      description: Customer создан
      schema:
        $ref: '#/definitions/CustomerAndToken'
    '400':
      $ref: '#/responses/BadRequest'
