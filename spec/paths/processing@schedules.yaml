get:
  description: Получить список расписаний выводов
  tags:
    - Payouts
  operationId: getSchedules
  parameters:
    - $ref: '#/parameters/requestID'
  responses:
    '200':
      description: List of schedules
      schema:
        type: array
        items:
          $ref: '#/definitions/Schedule'
