get:
  tags:
    - Участники
  operationId: getMyParty
  parameters:
    - $ref: '#/parameters/requestID'
  responses:
    '200':
      description: Get my party
      schema:
        $ref: '#/definitions/Party'
