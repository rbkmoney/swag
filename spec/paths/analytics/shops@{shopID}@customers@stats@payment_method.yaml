get:
  description: Получить данные о платежных средствах
  tags:
    - Analytics
  operationId: getPaymentMethodStats
  parameters:
    - $ref: '#/parameters/requestID'
    - $ref: '#/parameters/shopID'
    - $ref: '#/parameters/fromTime'
    - $ref: '#/parameters/toTime'
    - $ref: '#/parameters/splitUnit'
    - $ref: '#/parameters/splitSize'
    - name: paymentMethod
      in: query
      description: Payment method type
      type: string
      enum:
        - bankCard
  responses:
    '200':
      description: List of payment method stats
      schema:
        type: array
        items:
          $ref: '#/definitions/PaymentMethodStat'
    '400':
      $ref: '#/responses/BadRequest'
    '404':
      $ref: '#/responses/NotFound'
